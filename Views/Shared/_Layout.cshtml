<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - ALFINapp</title>
    @RenderSection("Styles", required: false)
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-version="true" />
    <link rel="stylesheet" href="~/css/styles.css" />
    <link rel="stylesheet" href="~/ALFINapp.styles.css" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.13/css/select2.min.css" rel="stylesheet" />
    <!-- Agregar el CSS de SweetAlert2 -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11.6.0/dist/sweetalert2.min.css" />

    <!-- ICONS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css" />

    <!-- AG GRID -->
    <script src="https://cdn.jsdelivr.net/npm/ag-grid-community/dist/ag-grid-community.min.js"></script>


    <!-- CUSTEM THEME -->
    <link rel="stylesheet" href="~/css/custom-theme.css" />


</head>




<!-- Layout.cshtml -->
<body class="vh-100 d-flex flex-column ba-color-light">
    <input type="checkbox" id="sidebarToggle" class="d-none" checked>
    <nav class="navbar navbar-dark ba-color-secondary py-2 mb-0">
        <div class="container-fluid gap-2">
            <div class="d-flex align-items-center gap-3">
                <label for="sidebarToggle" class="btn btn-outline-light rounded-circle sidebar-toggle-btn">
                    <i class="bi bi-layout-sidebar-inset"></i>
                </label>
                <span class="navbar-brand mb-0 fw-bold ba-color-light rounded-pill px-3 py-1 d-inline-flex align-items-center gap-2">
                    <i class="bi bi-bank2"></i>A365 / Alfin
                </span>
            </div>
            <div class="d-flex align-items-center">
                <button class="btn btn-danger rounded-pill d-inline-flex align-items-center gap-2">
                    <i class="ri-logout-circle-r-line"></i>
                    <span class="d-none d-md-block">Cerrar Sesión</span>
                </button>
            </div>
        </div>
    </nav>
    <div id="content" class="flex-grow-1 d-flex">
        <nav id="sidebar">
            <ul class="nav flex-column">
                <li class="nav-item">
                    <a href="#" class="nav-link active">
                        <i class="bi bi-speedometer2"></i>
                        <span class="menu-text">Dashboard</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="#" class="nav-link">
                        <i class="bi bi-people"></i>
                        <span class="menu-text">Clientes</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="#" class="nav-link">
                        <i class="bi bi-credit-card"></i>
                        <span class="menu-text">Transacciones</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="#" class="nav-link">
                        <i class="bi bi-calendar-check"></i>
                        <span class="menu-text">Agenda</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="#" class="nav-link">
                        <i class="bi bi-file-earmark-text"></i>
                        <span class="menu-text">Reportes</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="#" class="nav-link">
                        <i class="bi bi-graph-up"></i>
                        <span class="menu-text">Estadísticas</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="#" class="nav-link">
                        <i class="bi bi-gear"></i>
                        <span class="menu-text">Configuración</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="#" class="nav-link">
                        <i class="bi bi-question-circle"></i>
                        <span class="menu-text">Ayuda</span>
                    </a>
                </li>
            </ul>
        </nav>
        <div class="flex-grow-1 d-flex flex-column h-100 main-content" id="mainContent">
            <div class="flex-grow-1 p-3 bg-light content-wrapper">
                <div class="container-fluid py-3 content-scroll">
                    @RenderBody()
                </div>
            </div>
            <div class="shadow-sm py-3 border-top footer">
                <div class="container-fluid text-center">
                    <span class="text-muted">
                        © 2024 - AppBancoAlFIN - <a href="#">Privacy</a>
                    </span>
                </div>
            </div>
        </div>
    </div>


























    @* <!-- Navbar superior -->
    <nav class="navbar navbar-dark ba-color-primary p-2">
        <div class="container-fluid d-flex justify-content-between align-items-center">
            <!-- Toggle siempre visible (Req 1) -->
            <button class="btn btn-outline-light border-0"
                    id="sidebarToggler"
                    type="button"
                    data-bs-toggle="offcanvas"
                    data-bs-target="#sidebar"
                    aria-controls="sidebar">
                <i class="bi bi-list fs-5"></i>
            </button>

            <span class="navbar-brand d-flex align-items-center gap-2">
                <i class="bi bi-bank"></i> A365 / ALFIN
            </span>

            <button class="btn btn-danger">
                <i class="bi bi-box-arrow-right"></i>
                <span class="d-none d-md-inline">Cerrar Sesión</span>
            </button>
        </div>
    </nav>

    <!-- Contenedor principal: sidebar + main -->
    <div class="flex-grow-1 d-flex overflow-hidden">

        <!-- Sidebar: visible en MD+ y offcanvas en móvil -->
        <div id="sidebar-wrapper" class="d-none d-md-block ba-color-primary">
            <div class="h-100 d-flex flex-column">
                <div class="p-3 border-bottom border-light border-opacity-25">
                    <h5 class="mb-0 text-light">Menú</h5>
                </div>
                <div class="p-3">
                    <ul class="nav flex-column gap-2">
                        <li class="nav-item">
                            <a href="#" class="nav-link text-light d-flex align-items-center gap-2">
                                <i class="bi bi-house-door"></i> Inicio
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="#" class="nav-link text-light d-flex align-items-center gap-2">
                                <i class="bi bi-file-earmark-bar-graph"></i> Reportes
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="#" class="nav-link text-light d-flex align-items-center gap-2">
                                <i class="bi bi-gear"></i> Ajustes
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Versión offcanvas del sidebar para móvil (Req 3) -->
        <div class="offcanvas offcanvas-start d-md-none"
             tabindex="-1"
             id="sidebar"
             aria-labelledby="sidebarLabel"
             style="width: 250px;">
            <div class="offcanvas-header ba-color-primary">
                <h5 class="offcanvas-title text-light" id="sidebarLabel">Menú</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas" aria-label="Close"></button>
            </div>
            <div class="offcanvas-body ba-color-primary p-0">
                <div class="p-3">
                    <ul class="nav flex-column gap-2">
                        <li class="nav-item">
                            <a href="#" class="nav-link text-light d-flex align-items-center gap-2">
                                <i class="bi bi-house-door"></i> Inicio
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="#" class="nav-link text-light d-flex align-items-center gap-2">
                                <i class="bi bi-file-earmark-bar-graph"></i> Reportes
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="#" class="nav-link text-light d-flex align-items-center gap-2">
                                <i class="bi bi-gear"></i> Ajustes
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Main con scroll -->
        <main class="flex-grow-1 overflow-auto">
            <div class="p-3">
                @RenderBody()
            </div>
        </main>
    </div> *@



    

    @* <main role="main" class="flex-grow-1 d-flex flex-column">
        @RenderBody()
    </main>
    <footer class="@(ViewBag.isFooterAbsoluteAtBottom == true ? "tfoot-position-login" : "") text-center p-2">&copy; 2024 - AppBancoAlFIN - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a></footer>
    <!-- Agregar el script de SweetAlert2 -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.6.0/dist/sweetalert2.min.js?v=@DateTime.Now.Ticks"></script> *@
    <script src="~/lib/jquery/dist/jquery.min.js?v=@DateTime.Now.Ticks"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.13/js/select2.min.js?v=@DateTime.Now.Ticks"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.6.0/dist/sweetalert2.min.js?v=@DateTime.Now.Ticks"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.13/js/select2.min.js?v=@DateTime.Now.Ticks"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js?v=@DateTime.Now.Ticks"></script>
    <script src="~/js/site.js?v=@DateTime.Now.Ticks"></script>
    <script>
        // Toggle Sidebar
        document.getElementById('toggleSidebar').addEventListener('click', () => {
            const sidebar = document.getElementById('sidebar');
            const body = document.body;

            sidebar.classList.toggle('active');
            body.classList.toggle('sidebar-active');
        });

        // Cerrar sidebar al hacer clic fuera en móviles
        document.addEventListener('click', (e) => {
            if (window.innerWidth <= 768 &&
                !document.getElementById('sidebar').contains(e.target) &&
                !document.getElementById('toggleSidebar').contains(e.target)) {

                document.getElementById('sidebar').classList.remove('active');
                document.body.classList.remove('sidebar-active');
            }
        });
    </script>

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            $.ajax({
                url: '/Rol/Sidebar',
                type: 'GET',
                success: function (response) {
                    if (response.success === false) {
                        Swal.fire({
                            title: 'Error',
                            text: response.message,
                            icon: 'error',
                            confirmButtonText: 'Aceptar'
                        });
                        return;
                    } else {
                        $('#sidebarToggleRender').html(response); // Asegúrate de que el servidor retorne el HTML correcto
                    }
                },
                error: function (xhr, status, error) {
                    console.error("Error en la petición AJAX:");
                    console.error("Estado:", status);
                    console.error("Error:", error);
                    console.error("Respuesta del servidor:", xhr.responseText);

                    Swal.fire({
                        title: 'Error',
                        text: xhr.responseText || 'Error desconocido',
                        icon: 'error',
                        confirmButtonText: 'Aceptar'
                    });
                }
            });
        });
    </script>


    @await RenderSectionAsync("Scripts", required: false)
</body>

</html>
<script src="~/js/General/General.js?v=@DateTime.Now.Ticks"></script>
<script src="~/js/mainsupervisorfunctions.js?v=@DateTime.Now.Ticks"></script>
<script src="~/js/accountfunctions.js?v=@DateTime.Now.Ticks"></script>
<script src="~/js/generalfunctions.js?v=@DateTime.Now.Ticks"></script>
<script src="~/js/modificacionesfunctions.js?v=@DateTime.Now.Ticks"></script>
<script src="~/js/mainfunctions.js?v=@DateTime.Now.Ticks"></script>
<script src="~/js/commentfunctions.js?v=@DateTime.Now.Ticks"></script>
<script src="~/js/telefonosfunctions.js?v=@DateTime.Now.Ticks"></script>
<script src="~/js/tipificacionesfunctions.js?v=@DateTime.Now.Ticks"></script>
<script src="~/js/reagregacionesfunctions.js?v=@DateTime.Now.Ticks"></script>
<script src="~/js/accountfunctions.js?v=@DateTime.Now.Ticks"></script>
<script src="~/js/generalfunctions.js?v=@DateTime.Now.Ticks"></script>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        let observer = new MutationObserver(function (mutations, observer) {
            let menuItems = document.querySelectorAll(".sidebar-links .nav-link");

            if (menuItems.length > 0) {
                let path = window.location.pathname.toLowerCase();

                menuItems.forEach(link => {
                    let href = link.getAttribute("href").toLowerCase();
                    if (path.includes(href)) {
                        link.style.backgroundColor = "rgb(3, 40, 75)";
                        link.classList.add("w-100");
                    }
                });

                observer.disconnect();
            }
        });

        let sidebar = document.querySelector(".sidebar-links");
        if (sidebar) {
            observer.observe(sidebar, { childList: true, subtree: true });
        }
    });
</script>
